snapshots:
    - name: dim_hosts
      relation: ref('hosts')
      config:
        schema: gold
        database: AIRBNB
        unique_key: HOST_ID
        strategy: timestamp
        dbt_valid_to_current: "to_date('9999-12-31')"
        updated_at: HOST_CREATED_AT